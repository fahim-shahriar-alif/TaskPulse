import { PlaceholderPage } from '../components/PlaceholderPage'

export function TasksPage() {
  return (
    <PlaceholderPage
      eyebrow="Work"
      title="Tasks & Projects"
      description="Searchable list and Kanban views grouped by project, priority, and due date."
    />
  )
}
